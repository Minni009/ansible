- name: loops
  hosts: localhost
  tasks:
  - name: loops
    ansible.builtin.debug:
       msg: "hello {{item}}" #item is a defined keyword
    loops:
    - ramesh
    - suresh
    - Robert
    - malli
