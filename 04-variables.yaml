- name: variables and PLAY level
  hosts: localhost
  vars: 
     Course: "Devops"
     trainer: "Shiva"
     duration: "110hrs"

  tasks:
  - name: check variables
    ansible.builtin.debug:
    ansible.builtin.set_fact:
      msg: "Hi, {{Course}}"